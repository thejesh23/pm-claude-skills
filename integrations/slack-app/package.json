{
  "name": "pm-skills-slack",
  "version": "1.0.0",
  "private": true,
  "description": "Slack /skill slash command — run professional skills in Slack.",
  "scripts": { "deploy": "wrangler deploy", "dev": "wrangler dev" },
  "devDependencies": { "wrangler": "^3.0.0" }
}
